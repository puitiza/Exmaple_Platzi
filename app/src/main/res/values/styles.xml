<resources>

    <!-- Base application theme. -->
    <!--parent="Theme.AppCompat.Light.NoActionBar" esto es para no tener el toolbar, solo cambia el parent-->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        //Add this two line will fix your problem
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--Esto es como en web asignar un css-->
    <style name="RaisedbuttonDark" parent="Theme.AppCompat.Light">
        <item name="colorButtonNormal">@color/colorPrimaryDark</item>
        <item name="colorControlHighlight">@color/colorPrimary</item>
        <item name="android:textColor">@android:color/white</item>
    </style>
    
    <style name="EditTextWhite" parent="TextAppearance.AppCompat">
        <item name="android:textColorHint">@color/EditTextWhite</item>
        <item name="android:textSize">@dimen/edittext_txtsize_login</item>
        <item name="android:textColorPrimary">@color/EditTextWhite</item>
        <item name="colorAccent">@color/EditTextWhite</item>
        <item name="colorControlNormal">@color/EditTextWhite</item>
        <item name="colorControlActivated">@color/EditTextWhite</item>
    </style>

    <style name="EditTextBlack" parent="TextAppearance.AppCompat">
        <item name="android:textColorHint">@color/primary_text</item>
        <item name="android:textSize">@dimen/edittext_txtsize_login</item>
        <item name="android:textColorPrimary">@color/primary_text</item>
        <item name="colorAccent">@color/primary_text</item>
        <item name="colorControlNormal">@color/primary_text</item>
        <item name="colorControlActivated">@color/primary_text</item>
    </style>

</resources>
